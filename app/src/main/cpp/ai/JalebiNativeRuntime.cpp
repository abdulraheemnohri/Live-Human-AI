#include "JalebiNativeRuntime.h"

namespace LiveHumanAI {
// Runtime policy is intentionally implemented in the header so small Android
// builds can inline the hot decision path. This TU provides a stable NDK unit.
}
